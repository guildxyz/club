const incentiveKey = {
  rewardToken: process.env.NEXT_PUBLIC_REWARD_TOKEN_ADDRESS,
  pool: process.env.NEXT_PUBLIC_POOL_ADDRESS,
  startTime: process.env.NEXT_PUBLIC_START_TIME,
  endTime: process.env.NEXT_PUBLIC_END_TIME,
  refundee: process.env.NEXT_PUBLIC_REFUNDEE,
}

export default incentiveKey
